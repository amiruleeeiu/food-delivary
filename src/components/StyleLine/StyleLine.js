import React from 'react';
import './StyleLine.css'

const StyleLine = ({width}) => {
    return (
        <div className='style_line' style={{width}}>
            
        </div>
    );
};

export default StyleLine;